{% extends 'base.html.twig' %}

{% block title %}Haie{% endblock %}

{% block content %}
    <h1>Haie</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ haie.id }}</td>
            </tr>
            <tr>
                <th>Code</th>
                <td>{{ haie.code }}</td>
            </tr>
            <tr>
                <th>Nom</th>
                <td>{{ haie.nom }}</td>
            </tr>
            <tr>
                <th>Prix</th>
                <td>{{ haie.prix }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('haie_index') }}">back to list</a>

    <a href="{{ path('haie_edit', {'id': haie.id}) }}">edit</a>

    {{ include('haie/_delete_form.html.twig') }}
{% endblock %}
